{
  "scale_param": {
    "description": "指定清晰度倍数的参数名",
    "type": "string",
    "hint": "例如: -s 或 --scale。使用时: /ascii -s 1.5 或 /ascii --scale 1.5",
    "default": "--scale"
  },
  "charset_param": {
    "description": "指定字符集的参数名",
    "type": "string",
    "hint": "例如: -c 或 --charset。使用时: /ascii -c @#$% 或 /ascii --charset @#$%",
    "default": "--charset"
  },
  "chinese_param": {
    "description": "使用中文字符集的参数名",
    "type": "string",
    "hint": "例如: --chinese。使用时: /ascii --chinese",
    "default": "--chinese"
  },
  "default_scale": {
    "description": "默认清晰度倍数",
    "type": "float",
    "hint": "默认为1.0，即使用插件原有的自适应逻辑。数值越大越清晰，但处理时间也会相应增加",
    "default": 1.0
  },
  "default_charset": {
    "description": "默认字符集",
    "type": "string",
    "hint": "仅当未指定 --charset 时使用。英文字符推荐：@#S%?*+;:,. 或 @%#*+=-:. ",
    "default": "@#S%?*+;:,."
  },
  "default_chinese_charset": {
    "description": "默认中文字符集",
    "type": "string",
    "hint": "仅当使用 --chinese 且未指定字符时使用。推荐字符数 20-40 个",
    "default": "爱你喜欢我他她它好美帅酷炫酷帅呆了棒赞优强牛厉害威武霸气萌萌哒赞赞赞顶顶顶神神神"
  },
  "help_message": {
    "description": "帮助提示信息",
    "type": "text",
    "hint": "用户发送错误命令时显示的帮助信息",
    "default": "请引用一张图片并发送此命令\n\n使用方法:\n  /ascii - 默认清晰度(1.0倍)\n  /ascii --scale 1.5 - 提高清晰度(1.5倍)\n  /ascii --scale 0.5 - 降低清晰度(0.5倍)\n  /ascii --charset @#$ - 自定义字符集\n  /ascii --chinese - 使用中文字符\n\n可以组合使用多个参数"
  },
  "min_scale": {
    "description": "最小清晰度倍数限制",
    "type": "float",
    "hint": "用户指定的清晰度倍数不能小于此值",
    "default": 0.1
  },
  "max_scale": {
    "description": "最大清晰度倍数限制",
    "type": "float",
    "hint": "用户指定的清晰度倍数不能大于此值",
    "default": 5.0
  },
  "max_dimension": {
    "description": "输出图片最大边长限制",
    "type": "int",
    "hint": "输出图片的最大边长限制（像素），实际总像素数限制为该值的平方，不能超过硬编码的约5477像素限制",
    "default": 4096
  },
  "cache_cleanup_interval": {
    "description": "图片缓存清理间隔",
    "type": "int",
    "hint": "多久清理一次图片文件缓存，单位为分钟",
    "default": 60
  },
  "cache_max_age": {
    "description": "图片文件最大保留时间",
    "type": "int",
    "hint": "单个图片文件最多保留多久，单位为分钟。超过此时间的文件将被删除",
    "default": 1440
  },
  "auto_scale_adjust": {
    "description": "是否自动调整放大倍数",
    "type": "bool",
    "hint": "当放大倍数导致总像素数超过限制时，是否自动调整为最大允许的放大倍数",
    "default": true
  },
  "result_message": {
    "description": "转换结果消息文本",
    "type": "string",
    "hint": "图片转换成功后返回的消息文本",
    "default": "图片已转换为ASCII艺术:"
  }
}